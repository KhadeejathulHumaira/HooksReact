import React from 'react'
import './DispDetails.css'

function DispDetails(props) {
    return (
        <div>
            
            <h1>{props.det.name}</h1>
        </div>
    )
}

export default DispDetails
